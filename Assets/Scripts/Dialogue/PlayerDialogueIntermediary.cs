using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDialogueIntermediary : MonoBehaviour
{
    /*[SerializeField] public GameObject dialogueCanvas;
    [SerializeField] public GameObject pauseMenu;
    //public Gameobject playerChar;
    private DialogueCanvasManager dialogue;
    private PauseMenuManager menu;
    //private PlayerAnimationManager player;

    void Awake()
    {
        dialogue = dialogueCanvas.GetComponent<DialogueCanvasManager>();
        menu = pauseMenu.GetComponent<PauseMenuManager>();
        //player = playerChar.GetComponent<PlayerAnimationManager>();
        PlayerAnimationManager.raiseCaveDialogue += toCaveDialogue;
        PlayerAnimationManager.pause += pause;
        NPCInteractionManager.npcDialogue += npcDialogue;
    }
    void toCaveDialogue(PlayerAnimationManager pam)
    {
        Debug.Log("Entered the cave; starting dialogue.");
        dialogue.InitiateDialogueState("debug_knot.debug_stitch_1");
    }
    void pause(PlayerAnimationManager pam)
    {
        menu.InitConfigMenu();
    }
    void npcDialogue(NPCInteractionManager nim)
    {
        //{"npc_test_knot":["^test","\n",{"#f":1}]}
        Debug.Log("Starting NPC dialogue.");
        dialogue.InitiateDialogueState("debug_knot.debug_stitch_1");
    }*/
}
